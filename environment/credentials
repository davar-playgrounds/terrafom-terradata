# source this credentials file

PROFILE=thuy-qc

export AWS_ACCESS_KEY_ID=$(aws --profile $PROFILE configure get aws_access_key_id)
export AWS_SECRET_ACCESS_KEY=$(aws --profile $PROFILE configure get aws_secret_access_key)
